{
  "name": "com.xpeng.car",
  "version": "1.0.0",
  "main": "app.js",
  "scripts": {
    "lint": "eslint .",
    "clean": "rimraf .homeybuild",
    "prebuild": "npm run clean",
    "build": "homey app build",
    "build:production": "NODE_ENV=production homey app build",
    "validate": "homey app validate",
    "install:local": "homey app install",
    "deploy:production": "npm run build:production && npm run install:local",
    "test": "jest",
    "ci": "npm run lint && npm run test",
    "format": "prettier --write '**/*.{js,json,md}'"
  },
  "dependencies": {
    "node-fetch": "^2.7.0"
  },
  "devDependencies": {
    "@types/homey": "npm:homey-apps-sdk-v3-types@^0.3.9",
    "eslint": "^8.x.x",
    "eslint-config-athom": "^3.1.3",
    "jest": "^27.5.1",
    "prettier": "^2.x.x",
    "rimraf": "^5.0.1"
  },
  "engines": {
    "node": ">=12.0.0"
  }
}
